name = "qualityworkout-meal-analyze"
main = "src/index.js"
compatibility_date = "2026-01-01"

# Secrets — set with `wrangler secret put <NAME>`, never written here or
# committed anywhere:
#   ANTHROPIC_API_KEY   required — from https://console.anthropic.com
#   APP_SHARED_SECRET   optional but recommended — any random string you make up

[vars]
# Your GitHub Pages origin (scheme + host only, no path). Used for the CORS
# Access-Control-Allow-Origin header.
ALLOWED_ORIGIN = "https://lukeputrus.github.io"
MODEL_ID = "claude-haiku-4-5"
